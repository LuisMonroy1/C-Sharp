using System;

namespace Lecture2_23
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             * Program to check whether the given number is positive or negative 
             * (using ? : ternary operator )
             */

            /*
            To write this use if statement "if (n>0))" for possitive
            use if statement "if (n<0))" for negative
            */
            /* USING IF ELSE STATEMENTS
            int n;

            Console.WriteLine("Enter the number");
            n = Convert.ToInt32(Console.ReadLine());

            if (n > 0)
            {
                Console.Write("The number is positive");
            }

            else if (n < 0)
            {
                Console.Write("The number is negative");
            }
            else
            {
                Console.Write("The number is neither negative or positive");
            }
            */
            //USING VARIABLE RESULT (using ? : ternary operator )
            /*
            int n;

            Console.WriteLine("Enter the number");
            n = Convert.ToInt32(Console.ReadLine());

            var result = n > 0 ? "The number is positive" : "The number is negative";

                Console.Write(result);
            */


        }
    }
}
